# [CEB-BO-MEM-501-MD-LINK] 이모지 연결

## Page Properties

| Key | Value |
| ---- | ---- |
| Screen ID | CEB-BO-MEM-501-MD-LINK |
| 기획담당자 | @Nill Yoo |
| 디자인담당자 |  |
| 개발담당자 |  |
| 기능영역(Epic) | MEM — 기억저장소 / 감정 이모지 |
| 상태 | Draft |
| 버전 | v1.0 |
| 최근 업데이트 | 2026-06-08 |
| API |  |
| 피그마 링크 |  |
| 프로토타입 링크 | /memory/emoji |
| Breadcrumb | 홈 > 기억저장소 > 감정 이모지 연결 관리 |
| 비고 | 라이브러리에서 감정 이모지를 선택해 선택 아티스트에 연결하는 모달. [CEB-BO-MEM-501] 감정 이모지 연결 관리에서 호출. |

---

## 0. 공통 정책 참조

| 참조 문서 | 적용 범위 |
| --- | --- |
| [CEB-BO-000] 백오피스 공통 정책 | 모달 규격, 토스트, 활동 로그 |
| [CEB-000] 공통 정책 §7.5 감정 이모지 규격 | 라이브러리 연결 구조·연결 상한 (SSOT) |
| [CEB-BO-MEM-501] 감정 이모지 연결 관리 | 호출 화면·연결 규칙 |
| [CEB-BO-MEM-502] 감정 이모지 라이브러리 | 이모지 등록 원천 |

---

## 1. 화면 목적

라이브러리에 등록된 감정 이모지를 **선택해 현재 아티스트에 연결**하는 모달이다. 라이브러리에 원하는 이모지가 없으면 신규 등록 경로로 진입한다.

---

## 2. UI 설명

### 2.1 모달 구성

| 영역 | 내용 |
| --- | --- |
| 헤더 | "이모지 연결 — {아티스트명}" + [✕] 닫기 |
| 안내 | "라이브러리에서 연결할 이모지를 선택하세요. (연결 {N} / 40종)" |
| 검색 | 라벨(한국어·영어·일본어) 기준 검색 |
| 라이브러리 목록 | 이미지 썸네일 + 3언어 라벨 그리드. **다중 선택(체크)**. 이미 연결된 항목은 "연결됨" 배지 + 선택 비활성 |
| 신규 등록 진입 | "원하는 이모지가 없나요? [신규 등록]" → 등록 모달([CEB-BO-MEM-502-MD-EMOJI]) 열기. 등록 후 목록에 반영 |
| 푸터 | [취소] / [연결] (선택 0건이면 비활성) |

> 시각적 스타일은 프로토타입 참조

---

## 3. 화면 내 기능설명

| 요소 | 동작 |
| --- | --- |
| 항목 체크 | 연결할 이모지 다중 선택 |
| 검색 | 라벨 기준 목록 필터 |
| [신규 등록] | 라이브러리 등록 모달 열기 ([CEB-BO-MEM-502-MD-EMOJI]). 등록 완료 시 목록 갱신 |
| [연결] | 선택 항목을 현재 아티스트 세트에 연결(작업본 누적) + 모달 닫기 + 완료 토스트 |
| [취소] / [✕] | 선택 폐기 후 닫기 |

---

## 4. 규칙·로직

- 연결은 **작업본에 누적**되며, 앱 반영은 [CEB-BO-MEM-501]의 [변경사항 적용] 시점이다(즉시 반영 아님).
- 신규 연결 항목은 세트의 **마지막 순서**로 추가되며, 기본 **미사용**으로 연결된다(사용 6종 여유가 있으면 운영자가 [CEB-BO-MEM-501]에서 사용으로 전환).
- **이미 연결된 이모지는 중복 선택할 수 없다**(연결됨 배지·비활성).
- 연결 후 세트 총량이 **40종을 초과할 수 없다**. 남은 여유 칸을 초과하는 선택은 [연결] 차단 + 안내.
- [신규 등록]으로 라이브러리에 추가한 이모지도 본 모달에서 선택해야 아티스트에 연결된다(등록 자체는 연결이 아님).
- 권한은 [CEB-BO-SYS-102] 권한 관리 그룹 설정을 따른다.

---

## 5. UI 상태별 처리

| 상태 | 처리 |
| --- | --- |
| Default | 라이브러리 목록 표시(연결됨 항목 비활성) |
| Empty (라이브러리 0건) | "라이브러리에 등록된 이모지가 없습니다. [신규 등록]으로 추가하세요." |
| 선택 0건 | [연결] 비활성 |
| 여유 칸 초과 선택 | [연결] 차단 + "연결은 최대 40종까지 가능합니다" 안내 |
| 연결 중 | [연결] 비활성 |
| Error | Toast (Error) + 선택 유지 |

---

## 6. 활동 로그

| 액션 | 로그 메시지 (한국어) |
| --- | --- |
| 연결 | 감정 이모지 '{라벨}'을(를) 연결했습니다. (아티스트: {아티스트명}) |

> 다중 연결 시 항목별로 기록하거나 "{N}건 연결" 요약으로 기록(운영 로그 규격은 [CEB-BO-000] §6 준용).

---

## 7. 에러·예외 상황

| 상황 | 메시지 |
| --- | --- |
| 라이브러리 비어 있음 | "라이브러리에 등록된 이모지가 없습니다. [신규 등록]으로 추가하세요." |
| 여유 칸 초과 | "연결은 최대 40종까지 가능합니다." + [연결] 차단 |
| 연결 실패 | Toast (Error): "연결에 실패했습니다." + 선택 유지 |

---

## 8. 연관 화면ID

| 화면 ID | 화면명 | 관계 |
| --- | --- | --- |
| CEB-BO-MEM-501 | 감정 이모지 연결 관리 | 호출 화면 |
| CEB-BO-MEM-502 | 감정 이모지 라이브러리 | 선택 원천 |
| CEB-BO-MEM-502-MD-EMOJI | 감정 이모지 등록·수정 | [신규 등록] 진입 |

---

## 9. 변경 이력

| 버전 | 일자 | 작성자 | 변경 |
| ---- | ---- | ------ | --------- |
| **v1.0** | **2026-06-08** | **@Nill Yoo** | 신규 작성. 라이브러리에서 감정 이모지를 다중 선택해 아티스트에 연결하는 모달 — ① 이미 연결된 항목 비활성(중복 연결 차단) ② 신규 연결은 마지막 순서·미사용으로 추가 ③ 연결 40종 상한 검증 ④ 라이브러리에 없으면 [신규 등록] → [CEB-BO-MEM-502-MD-EMOJI] 진입 ⑤ 연결은 작업본 누적, 앱 반영은 [CEB-BO-MEM-501]의 [변경사항 적용] 시점. 구 [CEB-BO-MEM-501-MD-EMOJI](아티스트별 직접 등록)의 연결 역할을 대체. [CEB-000] §7.5 v6.5 정합. |
