# [CEB-BO-MEM-502] 감정 이모지 라이브러리

## Page Properties

| Key | Value |
| ---- | ---- |
| Screen ID | CEB-BO-MEM-502 |
| 기획담당자 | @Nill Yoo |
| 디자인담당자 |  |
| 개발담당자 |  |
| 기능영역(Epic) | MEM — 기억저장소 / 감정 이모지 |
| 상태 | Draft |
| 버전 | v1.0 |
| 최근 업데이트 | 2026-06-08 |
| API |  |
| 피그마 링크 |  |
| 프로토타입 링크 | /memory/emoji/library |
| Breadcrumb | 홈 > 기억저장소 > 감정 이모지 라이브러리 |
| 비고 | 감정 이모지를 아티스트와 무관하게 전역으로 등록·수정·삭제하는 화면. 등록된 이모지는 [CEB-BO-MEM-501]에서 아티스트에 연결한다. |

---

## 0. 공통 정책 참조

| 참조 문서 | 적용 범위 |
| --- | --- |
| [CEB-BO-000] 백오피스 공통 정책 | 권한, 활동 로그, UI 상태, 토스트 |
| [CEB-BO-MEM-000] 기억저장소 운영 영역 가이드 | 기억저장소 영역 정책 |
| [CEB-000] 공통 정책 §7.5 감정 이모지 규격 | 이미지 전용·기본 제공 6종·라이브러리 연결 구조 (SSOT) |
| [CEB-BO-MEM-501] 감정 이모지 연결 관리 | 아티스트별 연결 화면 |
| [CEB-BO-SYS-102] 권한 관리 그룹 설정 | 권한 그룹별 읽기·쓰기·삭제 범위 |

---

## 1. 화면 목적

회원이 기억 작성 시 선택하는 **감정 이모지를 전역으로 등록·수정·삭제**하는 화면이다.

- **누가**: 기억저장소 운영 권한이 있는 백오피스 관리자
- **왜**: 같은 감정 이모지를 여러 아티스트가 공유하므로, 이모지(이미지·라벨)는 아티스트와 무관하게 **한 번만 등록**해 라이브러리로 관리한다. 아티스트별 노출은 [CEB-BO-MEM-501]에서 연결로 처리
- **결과**: 라이브러리에 이모지를 등록·수정·삭제하고, 각 이모지가 몇 개 아티스트에 연결됐는지 확인

> 아티스트별 표시 순서·사용여부·연결은 이 화면이 아니라 [CEB-BO-MEM-501] 감정 이모지 연결 관리에서 수행한다.

---

## 2. UI 설명

### 2.1 화면 레이아웃

```
[Breadcrumb]  홈 › 기억저장소 › 감정 이모지 라이브러리          [이모지 등록]

전체 8종                                            [검색: 라벨 ▾]

┌──────────────────────────────────────────────────────────────┐
│ 이미지   라벨(한국어/영어/일본어)        연결 아티스트   관리      │
├──────────────────────────────────────────────────────────────┤
│ [🖼]     설렘 / Flutter / ときめき          5            수정·삭제 │
│ [🖼]     감동 / Moved / 感動                5            수정·삭제 │
│ [🖼]     특별기념 / Special / 特別           0            수정·삭제 │
│ ...                                                            │
└──────────────────────────────────────────────────────────────┘
```

### 2.2 상단 영역

| 요소 | 설명 |
| --- | --- |
| 전체 개수 | 라이브러리 등록 이모지 총 종수 |
| 검색 | 라벨(한국어·영어·일본어) 기준 검색 |
| [이모지 등록] | 등록 모달([CEB-BO-MEM-502-MD-EMOJI]) 열기 |

### 2.3 라이브러리 표 (컬럼)

| 컬럼 | 설명 |
| --- | --- |
| 이미지 | 등록된 이모지 이미지 썸네일 |
| 라벨 | 한국어 / 영어 / 일본어 라벨 |
| 연결 아티스트 | 이 이모지를 연결한 아티스트 수. 0이면 미연결(어디에도 노출되지 않음) |
| 관리 | [수정] (등록·수정 모달) / [삭제] (라이브러리 삭제 확인 모달). 연결 아티스트가 1개 이상이거나 회원 사용 이력이 있으면 [삭제] 비활성(툴팁으로 사유 안내) |

> 기본 제공 이미지 6종(설렘·감동·신남·사랑·우와·행복)도 라이브러리 항목으로 표시되며, 삭제 가능 조건은 동일하다.
> 시각적 스타일은 프로토타입 참조

---

## 3. 화면 내 기능설명

| 요소 | 동작 |
| --- | --- |
| [이모지 등록] | 등록 모달 열기 ([CEB-BO-MEM-502-MD-EMOJI]) |
| [수정] | 해당 이모지를 수정 모달로 열기 ([CEB-BO-MEM-502-MD-EMOJI]) |
| [삭제] | 라이브러리 삭제 확인 모달 ([CEB-BO-MEM-502-MD-DELETE]). **연결 아티스트 0 + 회원 사용 이력 없음**일 때만 활성 |
| 검색 | 라벨 기준 목록 필터 |

---

## 4. 규칙·로직

### 4.1 등록·수정

- 각 이모지는 **이미지 업로드**(정사각형 1:1 권장·JPG·PNG·WebP·5MB 이하)와 **3언어 라벨**(한국어·영어·일본어, 각 20자)로 구성한다. **라벨 중복 허용**.
- 이미지·라벨은 **전역 공유 속성**이다. **수정 시 이 이모지를 연결한 모든 아티스트의 앱 표시에 즉시 반영**된다(콘텐츠 정정 성격, [CEB-BO-MEM-501]의 작업본 적용과 무관).

### 4.2 삭제 (전역)

- 라이브러리 삭제는 이모지를 **전역에서 영구 제거**한다.
- **삭제 가능 조건**: **연결 아티스트가 0** 이고 **회원 사용 이력이 없는** 이모지만 [삭제]할 수 있다. 아래는 [삭제] 비활성(툴팁으로 사유 안내).
  - **연결 아티스트가 1개 이상** → 먼저 각 아티스트에서 연결 해제([CEB-BO-MEM-501-MD-UNLINK])해야 한다.
  - **회원이 이미 사용한 이모지** → 삭제 불가(기억 기록 보존). 연결 해제만 가능.
- 특정 아티스트에서만 빼려면 삭제가 아니라 [CEB-BO-MEM-501]에서 **연결 해제**한다.

### 4.3 권한

- 권한 그룹별 읽기·쓰기·삭제 범위는 [CEB-BO-SYS-102] 권한 관리 그룹 설정을 따른다.
- 등록·수정·삭제는 쓰기/삭제 권한 그룹만 사용한다.

---

## 5. UI 상태별 처리

| 상태 | 처리 |
| --- | --- |
| Loading | 라이브러리 표 골격 화면(Skeleton) 표시 |
| Default | 등록 이모지 목록 표시 |
| Empty (0건) | "등록된 감정 이모지가 없습니다. [이모지 등록]으로 추가하세요." 안내 |
| Error | "데이터를 불러오지 못했습니다." + [다시 시도] |
| Disabled (읽기 권한만) | 등록·수정·삭제 버튼 미노출 또는 회색 처리 |

---

## 6. 활동 로그

| 액션 | 로그 메시지 (한국어) |
| --- | --- |
| 등록 | 감정 이모지 '{라벨}'을(를) 라이브러리에 등록했습니다. |
| 수정 | 감정 이모지 '{라벨}' 정보를 수정했습니다. |
| 삭제 | 감정 이모지 '{라벨}'을(를) 라이브러리에서 삭제했습니다. |

---

## 7. 에러·예외 상황

| 상황 | 동작 |
| --- | --- |
| 이미지 미지정 | "이미지를 업로드하세요" + [저장] 비활성 |
| 이미지 형식·용량 위반 | "지원하지 않는 형식입니다(JPG·PNG·WebP)" / "이미지 용량은 5MB 이하만 등록할 수 있습니다" |
| 라벨 3언어 미입력 | [저장] 비활성 + 미입력 언어 탭에 빨간 점 |
| 삭제 불가 항목 | 연결 아티스트가 1개 이상이거나 회원 사용 이력이 있으면 [삭제] 버튼 비활성(클릭 불가, 툴팁으로 사유 안내) |
| 저장 실패 | Toast (Error): "처리에 실패했습니다." + 입력값 유지 |

---

## 8. 연관 화면ID

| 화면 ID | 화면명 | 관계 |
| --- | --- | --- |
| CEB-BO-MEM-000 | 기억저장소 운영 영역 가이드 | 상위 영역 가이드 |
| CEB-BO-MEM-501 | 감정 이모지 연결 관리 | 아티스트별 연결 화면 |
| CEB-BO-MEM-502-MD-EMOJI | 감정 이모지 등록·수정 | 하위 ([이모지 등록]·[수정]) |
| CEB-BO-MEM-502-MD-DELETE | 라이브러리 삭제 확인 | 하위 ([삭제]) |
| CEB-000 | 공통 정책 §7.5 | 감정 이모지 규격 |

---

## 9. 변경 이력

| 버전 | 일자 | 작성자 | 변경 |
| ---- | ---- | ------ | --------- |
| **v1.0** | **2026-06-08** | **@Nill Yoo** | 신규 작성. 감정 이모지 전역 라이브러리 화면 — ① 이미지 업로드(정사각형·JPG·PNG·WebP·5MB 이하) + 3언어 라벨로 이모지를 전역 등록·수정·삭제 ② 라이브러리 표(이미지·3언어 라벨·연결 아티스트 수·관리) + 라벨 검색 ③ 이미지·라벨 수정은 연결된 모든 아티스트에 즉시 전역 반영 ④ 삭제는 연결 아티스트 0 + 회원 사용 이력 없음일 때만(전역 제거), 특정 아티스트 제거는 [CEB-BO-MEM-501] 연결 해제 ⑤ 기본 제공 이미지 6종도 라이브러리 항목으로 노출. [CEB-000] §7.5 v6.5 정합. |
