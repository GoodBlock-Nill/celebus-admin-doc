# [CEB-BO-SYS-601] 관리자 활동 로그 컨텍스트

## Page Properties

| Key | Value |
| ---- | ---- |
| Screen ID | CEB-BO-SYS-601 |
| 기획담당자 | @Nill Yoo |
| 디자인담당자 | |
| 개발담당자 | |
| 기능영역(Epic) | SYS 관리자 — 활동 로그 + `artistContext` 컬럼·필터 |
| 상태 | Draft |
| 버전 | v1.0 |
| 최근 업데이트 | 2026.05.07 |
| 비고 | 전체 BO 활동 로그 통합 조회 + 멀티아티스트 컨텍스트 컬럼·필터 정책. [CEB-BO-100] §3 (관리자 활동 로그 = 전역 + 컨텍스트 컬럼). [CEB-BO-000] §6 활동 로그 표준의 멀티아티스트 확장. |

---

## 1. 공통 정책 참조

| 참조 문서 | 적용 |
| --- | --- |
| [CEB-BO-100] 멀티아티스트 운영 가이드 | **§3 관리자 활동 로그 = 전역 + 컨텍스트 컬럼**. 감사 추적은 통합, 검색은 분기 |
| [CEB-BO-000] §6 | 활동 로그 표준 — append-only / 3년 보관 / 영역별 메시지 템플릿 |
| [CEB-BO-005] 관리자(SYS) 영역 가이드 | 활동 로그 통합 조회 |
| [CEB-BO-100] §4 | EXTERNAL_ARTIST 활동 로그 차단 |

---

## 2. 화면 목적

전체 BO에서 발생한 운영자 활동 로그를 **통합 단일 테이블**로 조회한다. 멀티아티스트 운영을 반영해 `artistContext` 컬럼·필터를 추가하여, 특정 아티스트와 관련된 운영 활동을 분리 검색 가능하다.

> 본 화면은 SUPER 전용 감사 도구다. 일반 운영자는 본인 활동 로그만 [CEB-BO-SYS-201] 관리자 상세에서 조회.

---

## 3. 멀티아티스트 정책 적용

### 3.1 정책 결정 — 전역 + 컨텍스트 컬럼

[CEB-BO-100] §3 결정:

| 측면 | 채택 |
| --- | --- |
| 감사 통합성 | 모든 활동을 단일 테이블에 적재. 영역·아티스트별 분리 X |
| 검색 분기 | `artistContext` 컬럼 + 필터로 아티스트 단위 분기 가능 |
| EXTERNAL_ARTIST 접근 | ❌ 차단 ([CEB-BO-100] §4) |

### 3.2 `artistContext` 자동 기록 룰

활동 로그 적재 시 다음 룰로 `artistContext` 자동 기록:

| 작업 | `artistContext` 값 |
| --- | --- |
| 콘텐츠 생성·수정·삭제 (Quest/BIVE/모바일티켓 등) | 콘텐츠 소속 아티스트 ID |
| 회원 정지 처리 | `discoveryContext` ([CEB-BO-USR-501])와 동일 — 발견 컨텍스트 아티스트 ID |
| 헤더 글로벌 토글이 단일 아티스트인 상태에서 작업 | 토글 아티스트 ID |
| 글로벌 토글이 "전체" + 콘텐츠 무관 작업 (관리자 초대·권한 변경 등) | NULL (전역) |
| 마스터 데이터 변경 (그룹 등록·곡선 변경 등) | 변경 대상 아티스트 ID |

> 시스템이 자동 추론. 운영자가 수동으로 입력하지 않음.

---

## 4. UI 설명

### 4.1 화면 레이아웃

```text
┌──────────────────────────────────────────────────────────────┐
│ [홈 > 관리자 > 활동 로그]                                     │
│ # 관리자 활동 로그                                            │
├──────────────────────────────────────────────────────────────┤
│ [필터 영역]                                                   │
│  대분류(전체) ▾  중분류(전체) ▾  소분류(전체) ▾                │
│  운영자(전체) ▾  아티스트 컨텍스트(전체) ▾  ⭐ v1.0 신규       │
│  기간 [2026.05.01]~[2026.05.07]                               │
│  로그 검색 / IP 검색                                           │
│  [초기화] [엑셀 내보내기]                                     │
├──────────────────────────────────────────────────────────────┤
│ [집계 카드]                                                   │
│  필터 결과 N건 / 작성자 N명 / 영역 N개 / 아티스트 N개         │
├──────────────────────────────────────────────────────────────┤
│ [테이블]                                                      │
│  일시 | 운영자 | 대분류 | 중분류 | 소분류 | 메시지 | 컨텍스트 │
│         IP                                          ⭐ v1.0  │
│  --------------------------------------------------------    │
│  2026.05.07 14:32 | nill | FQ | Quest | 게시 |               │
│   Quest 'V01D 인증' 게시 | V01D | 211.234.180.105            │
├──────────────────────────────────────────────────────────────┤
│ [페이지네이션]                                                │
└──────────────────────────────────────────────────────────────┘
```

### 4.2 영역별 구성

| 영역 | 구성요소 | 비고 |
| --- | --- | --- |
| 헤더 | breadcrumb, 페이지 타이틀, 헤더 글로벌 토글 영향 받음 | |
| 필터 (행 1) | 대분류·중분류·소분류 dropdown (계층 종속) | [CEB-BO-000] §6.4 영역별 메시지 템플릿 표 |
| 필터 (행 2) | 운영자, **아티스트 컨텍스트(전체 / 등록된 그룹 / 전역(NULL))** ⭐ v1.0 신규 | |
| 필터 (행 3) | 기간(시작·종료, 기본 최근 7일, 최대 1년), 로그 텍스트 검색, IP 검색 | |
| 액션 | [초기화] / [엑셀 내보내기] | 엑셀은 SUPER만 |
| 집계 카드 | 필터 결과 건수·운영자 수·영역 수·아티스트 수 | |
| 테이블 | 8컬럼 — 일시·운영자·대분류·중분류·소분류·메시지·컨텍스트·IP | `컨텍스트` 컬럼 ⭐ v1.0 신규 |
| 페이지네이션 | 페이지당 50건 | |

### 4.3 테이블 컬럼

| 컬럼 | 설명 |
| --- | --- |
| 일시 | YYYY.MM.DD HH:mm:ss |
| 운영자 | 닉네임 (이메일) — 클릭 시 SYS-201 관리자 상세 |
| 대분류 | 영역 코드 (FQ·BIVE·USR·ART 등) |
| 중분류 | [CEB-BO-000] §6.4 영역별 |
| 소분류 | 액션 (생성·수정·삭제·게시·종료 등) |
| 메시지 | [CEB-BO-000] §6.4 표 템플릿 적용 |
| **컨텍스트** ⭐ v1.0 신규 | 아티스트 그룹 라벨 (V01D·iKON 등) 또는 "전역" (NULL) |
| IP | IPv4/IPv6 |

---

## 5. 화면 내 기능 설명

| 동작 | 결과 |
| --- | --- |
| 필터 변경 | 즉시 테이블·집계 카드 갱신. URL 동기화 |
| 행 클릭 | 활동 로그 상세 모달 (메시지 + 메타데이터 일괄) |
| 운영자 컬럼 클릭 | SYS-201 관리자 상세 |
| 컨텍스트 컬럼 클릭 | 해당 아티스트 ART 영역 그룹 상세로 이동 |
| 헤더 글로벌 토글 = 단일 아티스트 | 컨텍스트 필터에 토글 아티스트 자동 적용 (덮어쓰기 가능) |
| 엑셀 내보내기 | 현재 필터 결과 CSV 다운로드 (SUPER만) |
| 초기화 | 모든 필터 기본값 |

---

## 6. 규칙·로직

### 6.1 정렬 기준

- 기본: 일시 내림차순 (최신순).
- 일시 컬럼 헤더 클릭으로 정렬 토글.

### 6.2 컨텍스트 필터 옵션

| 옵션 | 동작 |
| --- | --- |
| 전체 | 모든 컨텍스트 (NULL 포함) |
| 특정 아티스트 그룹 | 해당 아티스트 컨텍스트 로그만 |
| 전역 (NULL) | 컨텍스트 NULL 로그만 (관리자 초대·권한 변경 등) |

### 6.3 데이터 보관

- [CEB-BO-000] §6 표준 — append-only, 3년 보관.
- 3년 초과 로그는 콜드 스토리지 자동 이관 (BO 화면 미노출. 별도 아카이브 도구 필요 시 후속 트랙).

### 6.4 EXTERNAL_ARTIST 차단

[CEB-BO-100] §4 적용:

- 본 화면 진입 차단.
- 자기 아티스트 활동만이라도 보고 싶다는 요구가 있을 경우 별도 정책 검토 (현재 본 가이드 범위 밖).

### 6.5 헤더 글로벌 토글 연동

- 토글 = 단일 아티스트일 때 본 화면 진입 시 컨텍스트 필터 자동 적용.
- 운영자가 페이지에서 "전체"로 덮어쓰기 가능 ([CEB-BO-100] §2.2).

### 6.6 권한 분기

| 권한 | 본 화면 접근 |
| --- | --- |
| SUPER | ✅ 전체 R + 엑셀 내보내기 |
| OPERATOR | ❌ 차단 (본인 활동만 SYS-201에서 조회) |
| POLICY | ❌ 차단 |
| EXTERNAL_ARTIST | ❌ 차단 |

---

## 7. 권한 매트릭스

| 권한 | R | W | D |
| --- | --- | --- | --- |
| SUPER | ✅ | — (append-only) | — (3년 보관, 삭제 불가) |
| OPERATOR | ❌ (본인 활동은 SYS-201) | ❌ | ❌ |
| POLICY | ❌ | ❌ | ❌ |
| EXTERNAL_ARTIST | ❌ | ❌ | ❌ |

---

## 8. 다국어 입력 정책

본 화면은 read-only — 입력 없음. 노출되는 메시지는 [CEB-BO-000] §6.4 영역별 템플릿(KO 고정).

---

## 9. 에러·예외 상황

| 상황 | 처리 |
| --- | --- |
| 데이터 0건 (필터) | "조건에 맞는 활동 로그가 없습니다." Empty |
| 1년 초과 기간 시도 | 자동 보정 + 토스트 |
| 3년 초과 로그 검색 | "최근 3년 이내만 조회 가능합니다. 그 이전 로그는 아카이브 문의." |
| 컨텍스트 필터 NULL 선택 | 컨텍스트 = NULL인 로그만 조회 (전역 작업) |
| EXTERNAL_ARTIST 직접 URL 접근 | "권한이 없습니다" + [홈으로] |
| 엑셀 내보내기 결과 10만 건 초과 | "내보내기 한도(10만 건)를 초과합니다." |

---

## 10. 활동 로그 발생 (메타)

본 화면 자체의 액션:

| 액션 | 메시지 |
| --- | --- |
| 활동 로그 엑셀 내보내기 | "활동 로그 엑셀 내보내기 (결과: N건, 필터: ...)" — 본 로그도 활동 로그에 기록 (재귀) |

> 활동 로그 변동 자체는 발생 영역에서 자동 적재 — 본 화면은 read-only.

---

## 11. 도입 시점

[CEB-BO-100] §6 로드맵:

- **Phase 1**: 데이터 모델에 `artistContext` 필드만 추가 (NULL 디폴트). 기존 활동 로그 그대로.
- **Phase 2**: 본 화면 활성화 — `artistContext` 자동 기록·필터·컬럼 노출. 기존 로그는 NULL로 backfill.
- **Phase 3**: 헤더 글로벌 토글과 자동 연동.

---

## 12. 연관 화면

| 화면ID | 관계 |
| --- | --- |
| [CEB-BO-SYS-201] | 관리자 상세 — 본인 활동 로그 (운영자별 단일 화면) |
| [CEB-BO-SYS-101] | 관리자 리스트 |
| [CEB-BO-100] | 멀티아티스트 마스터 §3, §4 |
| [CEB-BO-000] §6 | 활동 로그 표준 (대분류·중분류·소분류 + 메시지 템플릿) |
| [CEB-BO-005] | SYS 영역 가이드 |

---

## 13. 변경 이력

| 버전 | 일자 | 작성자 | 변경 내용 |
| --- | --- | --- | --- |
| v1.0 | 2026.05.07 | @Nill Yoo | 최초 작성. [CEB-BO-100] v1.1 §3·§4 결정 (활동 로그 = 전역 + 컨텍스트 컬럼) 구체화. `artistContext` 자동 기록 룰 5종 + 컨텍스트 필터 + 헤더 글로벌 토글 연동 + EXTERNAL_ARTIST 차단 + Phase 2 도입. SUPER 전용 감사 도구. |
