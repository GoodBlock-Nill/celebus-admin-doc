// 운영 사이트 클론 — 게임존·팬퀘스트 영역은 추후 영역별 클론 시 재추가
export const ITEMS_PER_PAGE = 20;

// Badge 컴포넌트의 기존 variant 호환을 위한 빈 config
export const GAME_STATUS_CONFIG = {} as Record<string, { label: string; bg: string; text: string }>;
export const GP_TYPE_CONFIG = {} as Record<string, { label: string; bg: string; text: string }>;
export const EXCHANGE_DIRECTION_CONFIG = {} as Record<string, { label: string; bg: string; text: string }>;
export const EXCHANGE_STATUS_CONFIG = {} as Record<string, { label: string; bg: string; text: string }>;
export const GAME_TYPE_BADGE_CONFIG = {} as Record<string, { label: string; bg: string; text: string }>;
export const QUEST_STATUS_CONFIG = {} as Record<string, { label: string; bg: string; text: string }>;
export const RAFFLE_STATUS_CONFIG = {} as Record<string, { label: string; bg: string; text: string }>;
export const SUBMISSION_STATUS_CONFIG = {} as Record<string, { label: string; bg: string; text: string }>;
export const REWARD_TYPE_CONFIG = {} as Record<string, { label: string; bg: string; text: string }>;
export const DELIVERY_TYPE_CONFIG = {} as Record<string, { label: string; bg: string; text: string }>;
